* 查看文件

  * 点击文件可以查看文件，默认先使用系统应用打开（已经实现）
  * 长按文件可以打开文件上下文菜单，可以选择用内置查看器打开（已经实现）
  * 内置查看器显示文本或者十六进制（已经实现）
  * 内置查看器最多显示4096字节的内容，在文本模式下，如果是多字节编码，显示到最有一个有效字符
  * 内置查看器使用异步方式加载，未加载完也可以退出（已实现：LaunchedEffect 在离开界面时取消）
  * 十六进制查看器同时显示十六进制内容和对应的ASCII字符，如果ASCII字符不可见，则显示为点（已实现）
  * 实现文件拷贝和移动，这个功能这样实现：长按文件或者目录，可以把目录加入到待处理列表，而在某个目录下，可以选择移动到本处或者拷贝到本处。提供一个界面允许编辑待处理列表。（已实现）
  * 在显示文件的列表中，为已经在待处理列表中的文件加上一个标记，以便区分（已实现）

* 文件维护

  * 支持文件和目录的创建，删除，改名，移动和拷贝
  * 支持显示通过正则表达式对显示的内容进行过滤

* gnupg加解密功能

  * 在上下文菜单下可以选择对文件进行gnupg加密或者解密，可以直接使用对称或者非对称加密
  * 在软件的默认存储上可以保存gnupg的公钥和私钥，可以产生本地默认私钥
