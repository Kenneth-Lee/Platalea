* 优化待处理列表：待处理列表是本应用批量处理文件的关键，所有要整批处理的文件，都先加入这个列表，然后对列表的文件或者文件夹进行逐一处理。我们需要对这个功能进行界面和功能的优化，达到最好的使用体验。这些功能实现的时候尽量复用处理单个文件一样的逻辑。

  * ~~优化选择功能：双击文件可以把它加入待处理列表，再双击可以把它从待处理列表中移除~~
  * ~~待处理列表菜单变成一个点击按钮，点击直接打开待处理列表，原来待处理列表菜单中的“移动到此处”，“拷贝到此处”等功能，都放到待处理列表内部，这样用户直接可以看到待操作的列表是什么样的。~~
  * ~~“删除待处理列表中的文件”这个功能也放到和“移动到此处”，“拷贝到此处”一样的地位上。这样用户直接看到当前列表，也可以直接决定是否删除整个列表的内容了~~
  * ~~在待处理列表界面上显示当前目录，然后把“移动到此处”，“拷贝到此处”和“删除”功能显示成图标，再加上一个信息按钮，点击可以说明所有按钮的功能。~~
  * ~~待处理列表的处理功能加上：混淆和去混淆列表中的文件~~
  * ~~待处理列表的处理功能加上：加密或者解密列表中的文件，如果文件列表中既有.gpg文件，也有非.gpg文件，可以直接拒绝操作~~

* ssh数据交换功能：提供一个临时的ssh服务器，仅支持scp协议，允许同一局域网的其他机器从本服务器拷贝，或者拷贝文件到本服务器上。
* 网络数据交换功能：利用互联网上的网络服务，提供临时上传文件，或者从服务上取回的能力。
* 家庭日记功能
