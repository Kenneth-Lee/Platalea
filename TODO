以下功能后续立即进入开发

* 播放器使用界面优化
  * 在“最近”页面上选择播放，如果对应的播放列表已经在播放了，不应该打断这个播放，
    因为只是选择“继续这个播放”，而不应该理解为“从上次的位置重新开始这个播放”。
  * 在配置中增加一个功能，可以在桌面增加两个快捷方式，通过这些快捷方式进入应用，
    分别直接播放器页面或者快速笔记界面
  * 播放器的控制按钮做大放到底部，以便用户可以最快看到并很容易做出控制动作。
  * 进入播放列表后，如果当前列表正在播放，增加播放状态面板，可以看到播放进度。
    和主播放界面一样，控制按钮做大放到底部，以便用户可以直接控制。当切换上一首
    或者下一首的时候，把列表中的歌曲滚动到当前播放那一首。

* epub阅读器放大的倍数，针对每个被打开的文件（包括那些用epub阅读器打开的
  .txt/.llm/.llm.zip文件），可以持久化，下次打开可以保持。

* 词典发生过查询，保留10条历史记录

以下功能等我想清楚再做（本开发阶段不考虑）：

* pdf的内容可以读取（现在主要把引入外置库成本很高，那不如用另一个软件单独做）
