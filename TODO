* bug修复：从其他应用打开文件，选择本程序后，文件可以保存到当前目录，但界面没有刷新，导致该目录看不到
* bug修复：对一个文件执行私钥签名失败，调试日志显示：

	[GPG] 私钥签名失败: linux.txt
	异常: org.pgpainless.exception.WrongPassphraseException: Wrong passphrase provided for key ae8f3ab1e81a8da5

* ftp数据交换窗口的日志在窗口退出后要清除，下次进入重新记录
* 主文件列表的过滤窗口，本次运行关闭后，下次启动又会显示出来，要把这个选项做成一个持久化的配置
* ftp密码的配置要用星号显示，不能显示出来
* ftp数据交换功能放一个倒计时，时间到了自动退出，这个倒计时可以配置，以分钟为单位，可以配置为0表示用不退出
* 优化文件列表的图标，对于系统认识的文件类型，使用相应的图标显示，另外，加密的文件要显示成红色，混淆的文件要显示成蓝色。
* 提供文件列表排序功能，可以选择按更新时间，大小，文件名进行排序

以下功能等我想清楚再做（本开发阶段不考虑）：

* 上传网盘功能（特别是加密后自动上传）
* 访问一个公共的git库，持续同步一个项目的内容的功能
* 基于公共的git库，把同步的某个目录的信息展现为家庭日记的功能
* 基于某个agent，管理家庭日记的功能（要有一个既用git库，又保证经过加密的能力）
* 改进文本文件编辑功能，让它对md文件编辑极为友好（考虑用不同的固定模板做固定的事情，比如日程，经历，等等）
* 提供在后台播放某个目录的所有mp3的功能
* 提供一个md文件的渲染显示功能
