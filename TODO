以下功能后续立即进入开发

* 优化加解密使用界面（已完成）
  * 加密的时候，不需要询问对称还是非对称，而是直接显示非对称的可选公钥和密码输入，如果用户选择公钥，那就是非对称加密，选择密码，那就是对称加密
  * 解密的时候，也不需要询问对称还是非对称，可以直接通过文件格式判定，对称加密的文件自动提示密码，非对称的自动用私钥解密

* 优化播放器使用界面（已完成）
  * 播放界面要显示音乐当前乐曲的总时长和当前时长
  * 播放界面和恢复播放按钮和播放图标功能重复了，删除前者
  * 支持播放列表也要放到配置的导出中

* .pci.zip图片显示优化（已完成）
  * 在图片之间切换的时候，现在显示新图前会先清空屏幕，这导致在每次图片切换的时候，屏幕会闪烁一下，请考虑让新的图直接覆盖旧图，避免闪烁
  * 在图片显示框上增加一个按钮，可以把文件保存到根目录上
  * 图片浏览界面加上“保存到本地”功能

* rst渲染优化（已完成）
  * 注释不要显示
  * 支持.. image::语法
  * 支持表格
  * 支持.. list-table::语法
  * 支持语法高亮，至少支持java/kotlin, c/c++，python, json等语法
  * 支持.. [#second]类型的脚注

以下功能等我想清楚再做（本开发阶段不考虑）：


* pdf的内容可以读取（现在主要把引入外置库成本很高，那不如用另一个软件单独做）
* 支持matplotlib绘图：选择一个.py文件，让它把matplotlib绘制到屏幕上。（这个分析结论是成本太高，不做的好）
  下面是AI的分析：

  可行性上，真正靠谱的方案基本只有一条：在 APK 里嵌入 Python 运行时，再让
  matplotlib 走无界面后端，把图渲染成 PNG，然后交给现有图片或 WebView 界面显示。
  技术上能做，但代价不小。你现在的 APK 产物大约是 20M 的 release、27M 的 debug，
  而 build.gradle.kts 里没有做 ABI 拆分，也没有启用 release 混淆压缩；这意味着
  只要引入 Python 运行时、numpy、matplotlib 这类库，体积会比较实在地长上去。保
  守估计，release 很可能从 20M 涨到 50M 到 90M 这个区间；如果把多个 ABI 一起打
  包，甚至还可能更高。这个量级已经不是“加一个普通依赖”的影响了。

  速度和资源成本也会比较明显。第一次初始化 Python 运行时通常就会有一次肉眼可见
  的等待；第一次 import numpy 和 matplotlib 往往比普通文件打开慢一个量级，在中
  低端手机上出现 1 到 5 秒的首开等待并不奇怪。内存方面，单是把 Python、numpy、
  matplotlib 拉起来，进程常驻内存增加几十 MB 很正常；如果脚本画大图、子图很多、
  数据量大，峰值内存到 100MB 以上也不意外。对你这个现在以文件管理、查看、轻量编
  辑为主的应用来说，这会是一次明显的产品定位变化。

  还有几个实际风险需要提前看到。第一，兼容性。很多 matplotlib 脚本默认会依赖桌
  面环境、字体、交互后端、pandas、scipy，甚至本地文件路径写法；这些在 Android
  上经常不成立。第二，安全性。只要支持“选择一个 .py 文件并执行”，本质上就是在应
  用里跑用户脚本；哪怕只是本地脚本，也要考虑死循环、超大内存分配、读写任意根目
  录文件、卡死界面这些问题。第三，维护成本。你后面会多出一整套 Python 版本、包
  版本、ABI、崩溃日志、脚本沙箱、超时终止的维护问题，这个复杂度会高于当前仓库里
  大多数功能。

  如果只看“有没有更轻的做法”，有，但都不是完整的 matplotlib 支持。最轻的是只支
  持显示脚本同目录里已经存在的 PNG、SVG，或者约定脚本输出到某个文件后由应用展示，
  这对体积和速度影响几乎可以忽略，但它不是“手机里执行 matplotlib”。另一种是做一
  个非常受限的“绘图脚本格式”，比如只支持少量折线图、柱状图参数，然后用 Android
  原生Canvas 或 WebView 图表库画，这样体积小、速度快，但它也不是兼容现有
  matplotlib代码。至于用 WebView 跑 Pyodide 这类浏览器端 Python，理论上能跑，实
  际上比内嵌Python 还重，启动更慢，内存更高，不适合这个项目。
