* 在ftp数据交换界面显示还剩多少分钟关闭窗口，该时间动态更新

* 实现git功能，思路如下：
  * 可以配置git参数，从比如github.com, atomgit.com等git服务商上下载git库，整个app只需要下载特定的一个库就行，不需要做通用的功能
  * 支持ssh方式下载即可，不需要支持多种下载形式，要可以导入本地需要的私有key
  * 尽量使用成熟的git访问开发库，不要从0开始开发，但要保证开发库支持push，因为未来需要上传内容
  * git库下载到固定的位置，比如根目录的.sysgit子目录下

以下功能等我想清楚再做（本开发阶段不考虑）：

* 上传网盘功能（特别是加密后自动上传）
* 访问一个公共的git库，持续同步一个项目的内容的功能
* 基于公共的git库，把同步的某个目录的信息展现为家庭日记的功能
* 基于某个agent，管理家庭日记的功能（要有一个既用git库，又保证经过加密的能力）
* 改进文本文件编辑功能，让它对md文件编辑极为友好（考虑用不同的固定模板做固定的事情，比如日程，经历，等等）
* 提供在后台播放某个目录的所有mp3的功能
