以下功能后续立即进入开发

* 播放器使用界面优化
  * [已实现] 在“最近”页面上选择播放时，如果对应播放列表已经在播放，不再打断当前播放；
    此时仅跳转到播放器页面，相当于“继续当前播放”。
  * [已实现] 在配置中增加“创建桌面快捷方式”功能，可分别直达播放器页面和速记页面。
  * [已实现] 播放器控制按钮已改为底部大按钮布局，便于快速触达和操作。
  * [已实现] 进入播放列表后，若该列表正在播放，会显示播放状态面板（含进度/拖动）；
    同时提供与主播放界面一致的底部大控制按钮，并在上一首/下一首后自动滚动到当前播放歌曲。

* [已实现] epub阅读器放大的倍数，针对每个被打开的文件（包括那些用epub阅读器打开的
  .txt/.llm/.llm.zip文件），可以持久化，下次打开可以保持。

* [已实现] epub阅读界面在内容左边30%的空间点击回到上一章，在内容右边30%的空间点击跳到下一章。

* [已实现] 词典发生过查询，保留10条历史记录

* [已实现] 播放器被电话或者其他应用介入时，改为通过系统音频焦点暂停；在占用音频
  的应用退出并归还焦点后，仅对这类“被系统打断”的暂停自动恢复播放。

* 现在输入密码的界面都是用输入法输入的，密码可能被输入法记录在缓存中，我希望设
  计一个统一的界面，用于输入密码。这个界面只允许输入数字，字母或者标准的字符
  （比如~!@#等），输入的结果也只回显*号，不会显示在屏幕上。但有时别人发过来的加
  密文件可能用了中文，这个输入窗口允许从外部拷贝内容作为密码，这样极端情况下需
  要输入一般字符之外的密码，也可以临时通过剪贴板拷贝进来。

以下功能等我想清楚再做（本开发阶段不考虑）：

* pdf的内容可以读取（现在主要把引入外置库成本很高，那不如用另一个软件单独做）
