
以下功能等我想清楚再做（本开发阶段不考虑）：

* pdf的内容可以读取
* md.zip等文件支持编辑
* md渲染如果遇到注释语法，要显示注释标记，同时点上去应该弹出一个提示，显示注释内容。比如“[^]”标记了一个内部的注解，应该在注解点上显示一个链接，点上去直接显示具体定义的那个注解。
* md支持删除线语法，比如这样“~~~xxxx~~~”，应该显示为xxxx的删除线。
* md支持勾选语法，比如“[]"表示任务未勾选，"[x]"表示任务已经被勾选。
